[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["cd server && npm ci --only=production"]

[start]
cmd = "cd server && node index.js"

[phases.build]
cmds = ["echo 'Skipping build for API-only deployment'"]
